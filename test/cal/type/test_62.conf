HEADERS=H1 INTEGER, H2 BOOL, H3 DOUBLE, H4 CHAR(4), H5 VARCHAR(100), H6 DATETIME
SORT_HEADERS=H1
SORT_ORDER=ASC
CSV_FORMULA=Print H6[0] +  H2[0]
CSV_FORMULA=Print H6[0] *  H2[0]
CSV_INPUT=test.input
CSV_OUTPUT=test.out
CSV_ERROR=test.err
CSV_RESULT=test.res
